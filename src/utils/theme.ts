import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#F47C4B',
      contrastText: '#ffffff',
    },
  },
  typography: {
    fontFamily: 'Noto Serif',
  },
});